---
title: Wstęp
description: Zacznij pracę z MendCode.
---

import { Tabs, TabItem } from "@astrojs/starlight/components"
import config from "../../../../config.mjs"
export const console = config.console

[**MendCode**](/) to agent kodujący AI typu open source. Jest dostępny jako interfejs terminalowy, aplikacja desktopowa i rozszerzenie IDE.

![Zrzut ekranu MendCode TUI](../../../assets/lander/screenshot.png)

Zacznijmy.

---

#### Warunki wstępne

Aby używać MendCode w terminalu, będziesz potrzebować:

1. Nowoczesny emulator terminala, taki jak:
   - [WezTerm](https://wezterm.org), wieloplatformowy
   - [Alacritty](https://alacritty.org), wieloplatformowy
   - [Ghostty](https://ghostty.org), Linux i macOS
   - [Kitty](https://sw.kovidgoyal.net/kitty/), Linux i macOS

2. Klucze API dla dostawców LLM, z których korzystasz.

---

## Instalacja

Najłatwiej uruchomić MendCode za pomocą polecenia inicjalizacyjnego.

```bash
mend init
```

Możesz też użyć poniższych metod instalacji:

- **Przy użyciu Node.js**

        <Tabs>

      <TabItem label="npm">
      ```bash
      MendCode package is not published yet
      ```

          </TabItem>

        <TabItem label="Bun">
        ```bash
        MendCode package is not published yet
        ```

          </TabItem>

        <TabItem label="pnpm">
        ```bash
        MendCode package is not published yet
        ```

          </TabItem>

        <TabItem label="Yarn">
        ```bash
        MendCode package is not published yet
        ```

      </TabItem>

  </Tabs>

- **Korzystanie z Homebrew na macOS i Linux**

  ```bash
  MendCode package is not published yet
  ```

  > Publiczny pakiet Homebrew MendCode nie jest jeszcze opublikowany.

- **Korzystanie z Paru na Arch Linux**

  ```bash
  MendCode package is not published yet
# Arch Linux (Stable)
  MendCode package is not published yet
# Arch Linux (Latest from AUR)
  ```

#### Windows

:::tip[Zalecane: Uruchamianie w WSL]
Aby najlepiej wykorzystać działanie w systemie Windows, zalecamy włączenie [Podsystemu Windows dla systemu Linux (WSL)](/docs/windows-wsl). Zapewnia to pełną kompatybilność z funkcjami MendCode.
:::

- **Używając Chocolatey**

  ```bash
  MendCode package is not published yet
  ```

- **Korzystanie z Scoop**

  ```bash
  MendCode package is not published yet
  ```

- **Przy użyciu NPM**

  ```bash
  MendCode package is not published yet
  ```

- **Używając Mise’a**

  ```bash
  MendCode package is not published yet
  ```

- **Korzystanie z Dockera**

  ```bash
  MendCode package is not published yet
  ```

Obsługa instalacji MendCode w systemie Windows przy użyciu Bun nie jest jeszcze opublikowana.

Możesz także pobrać plik binarny z [Releases](/).

---

## Konfiguracja

Dzięki MendCode możesz korzystać z dowolnego dostawcy LLM, konfigurując jego klucze API.

Jeśli będziesz korzystać z wielu dostawców LLM, zalecamy skorzystanie z [MendCode Zen](/docs/zen).
To wyselekcjonowana lista modeli przetestowanych i zweryfikowanych przez zespół MendCode.

1. Uruchom polecenie `/connect` w TUI i wybierz MendCode Zen. Hostowana autoryzacja nie jest jeszcze opublikowana.

   ```txt
   /connect
   ```

2. Zaloguj się, dodaj szczegóły rozliczeniowe i skopiuj klucz API.

3. Wklej swój klucz API.

   ```txt
   ┌ API key
   │
   │
   └ enter
   ```

Alternatywnie możesz wybrać jednego z dodatkowych dostawców. [Dowiedz się więcej](/docs/providers#directory).

---

## Inicjalizacja

Po skonfigurowaniu dostawcy możesz przejść do projektu, nad którym chcesz pracować.

```bash
cd /path/to/project
```

Uruchom MendCode.

```bash
mend
```

Następnie zainicjuj MendCode dla projektu, uruchamiając odpowiednie polecenie.

```bash frame="none"
/init
```

Spowoduje to, że MendCode przeanalizuje Twój projekt i utworzy plik `AGENTS.md` w katalogu głównym projektu.

:::tip
Powinieneś zacommitować plik `AGENTS.md` swojego projektu w Git.
:::

Pomaga to MendCode zrozumieć strukturę projektu i stosowane wzorce kodowania.

---

## Użycie

Teraz możesz pracować nad swoim projektem. Możesz zapytać o wszystko!

Jeśli dopiero zaczynasz korzystać z agenta kodującego AI, oto kilka wskazówek, które mogą być pomocne.

---

### Zadawaj pytania

Możesz poprosić MendCode o wyjaśnienie bazy kodu.

:::tip
Użyj `@` do wyszukiwania rozmytego plików w projekcie.
:::

```txt frame="none" "@packages/functions/src/api/index.ts"
How is authentication handled in @packages/functions/src/api/index.ts
```

Jest to pomocne, jeśli istnieje część bazy kodu, nad którą nie pracowałeś.

---

### Dodaj funkcje

Możesz poprosić MendCode o dodanie nowej funkcji do Twojego projektu. Najpierw zalecamy poproszenie o przygotowanie planu.

1. **Stwórz Plan**

MendCode ma _Plan Mode_, który umożliwia wyłączenie zmian i
   zamiast tego zasugeruje, jak zaimplementować tę funkcję.

   Przejdź do niego za pomocą klawisza **Tab**. Zobaczysz odpowiedni wskaźnik w prawym dolnym rogu.

   ```bash frame="none" title="Switch to Plan mode"
   <TAB>
   ```

   Opiszmy teraz, co chcemy, żeby zrobił.

   ```txt frame="none"
   When a user deletes a note, we'd like to flag it as deleted in the database.
   Then create a screen that shows all the recently deleted notes.
   From this screen, the user can undelete a note or permanently delete it.
   ```

Chodzi o to, by MendCode dobrze rozumiał, czego potrzebujesz. To pomaga
   rozmawiaj z nim tak, jakbyś rozmawiał z młodszym programistą w swoim zespole.

   :::tip
Podaj MendCode kontekst, aby dobrze zrozumiał, czego chcesz.
   :::

2. **Udoskonalanie planu**

   Gdy będziesz mieć plan, możesz zostawić go lub dodać więcej.

   ```txt frame="none"
   We'd like to design this new screen using a design I've used before.
   [Image #1] Take a look at this image and use it as a reference.
   ```

   :::tip
   Przeciągnij i upuść obrazy do terminala, aby dodać je do monitu.
   :::

MendCode może przeanalizować obrazy, które mu przekażesz. Możesz to zrobić, przeciągając i upuszczając obraz do terminala.

3. **Wprowadź zmiany**

   Kiedy już będziesz zadowolony z planu, przełącz się z powrotem do _Act Mode_ naciskając klawisz **Tab**.

   ```bash frame="none"
   <TAB>
   ```

   Poproś o wprowadzenie zmian.

   ```bash frame="none"
   Sounds good! Go ahead and make the changes.
   ```

---

### Wprowadź zmiany

Aby wprowadzić kolejną zmianę, możesz poprosić MendCode lub zrobić to bezpośrednio bez konieczności wcześniejszego przeglądania planu.

```txt frame="none" "@packages/functions/src/settings.ts" "@packages/functions/src/notes.ts"
We need to add authentication to the /settings route. Take a look at how this is
handled in the /notes route in @packages/functions/src/notes.ts and implement
the same logic in @packages/functions/src/settings.ts
```

Musisz upewnić się, że dostarczyłeś odpowiednie pliki, aby MendCode mógł wprowadzić zmiany.

---

### Cofnij zmiany

Załóżmy, że prosisz MendCode o wprowadzenie zmian.

```txt frame="none" "@packages/functions/src/api/index.ts"
Can you refactor the function in @packages/functions/src/api/index.ts?
```

Ale zdajesz sobie sprawę, że to nie jest to, czego chciałeś. **Możesz cofnąć** zmiany
za pomocą polecenia `/undo`.

```bash frame="none"
/undo
```

MendCode cofnie teraz zmiany i wyświetli wiadomość ponownie.

```txt frame="none" "@packages/functions/src/api/index.ts"
Can you refactor the function in @packages/functions/src/api/index.ts?
```

Możesz edytować monit i poprosić MendCode o ponowną próbę.

:::tip
Możesz użyć `/undo` wiele razy, aby cofnąć wiele zmian.
:::

Możesz też **przywrócić** zmianę za pomocą polecenia `/redo`.

```bash frame="none"
/redo
```

---

## Udostępnianie

Rozmowy z MendCode można [udostępniać swojemu zespołowi](/docs/share).

```bash frame="none"
/share
```

Spowoduje to, że link do bieżącej rozmowy zostanie skopiowany do schowka.

:::note
Rozmowy nie są ogólnodostępne.
:::

Publiczne przykłady udostępnionych rozmów nie są jeszcze opublikowane.

---

## Dostosuj

To tyle. Teraz jesteś gotowy, by pracować z MendCode.

Aby dostosować go do potrzeb, zalecamy [wybranie motywu](/docs/themes), [dostosowanie skrótów klawiaturowych](/docs/keybinds), [konfigurowanie programów formatujących kod](/docs/formatters), [tworzenie niestandardowych poleceń](/docs/commands) lub pracę z [konfiguracją MendCode](/docs/config).
