Send a plain-text message to one explicit MendCode session in the same project.

This queues an auditable peer message for the target session. It never shares history, files, reasoning, permissions, or model context. The target must explicitly accept the message before it is delivered to its prompt queue; if the target is busy, delivery waits for its next idle boundary.
